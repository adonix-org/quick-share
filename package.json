{
    "name": "share-link",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "cf-typegen": "wrangler types"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.8.67",
        "typescript": "^5.9.2",
        "vitest": "~3.2.4",
        "wrangler": "^4.33.1"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.99"
    }
}
