{
    "name": "share-link",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "types": "wrangler types ./types/wrangler.d.ts"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.9.12",
        "@types/he": "^1.2.3",
        "typescript": "^5.9.3",
        "vitest": "~3.2.4",
        "wrangler": "^4.42.2"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.177",
        "he": "^1.2.0"
    }
}
